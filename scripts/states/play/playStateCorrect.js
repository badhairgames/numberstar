import { PlayState } from "./playState.js";

class PlayStateCorrect extends PlayState {
    constructor(parent) {
        super(parent);
    }

    setup() {
    }

    update(elapsed) {
    }

    draw() {
    }

    teardown() {
    }
}

export { PlayStateCorrect };